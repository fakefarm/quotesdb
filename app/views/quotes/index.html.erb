<h1>Dave Woodall's first Ruby on Rails database</h1>

<h2>Listing Quotes</h2>

<table>
  <tr>
    <th>Quote</th>
    <th>By</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @quotes.each do |quote| %>
  <tr>
    <td><%= quote.quote %></td>
    <td><%= quote.by %></td>
    <td><%= link_to 'Show', quote %></td>
    <td><%= link_to 'Edit', edit_quote_path(quote) %></td>
    <td><%= link_to 'Destroy', quote, :confirm => 'Do you really want to delete this great quote?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Quote', new_quote_path %>
